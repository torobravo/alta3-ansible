# Building and using an Ansible role
Roles are an essential feature in Ansible that help organize and reuse automation tasks.

Ansible provides a handy command to generate a role template. Run the following command:
```bash
    ansible-galaxy role init ~/mycode/roles/arista_backup
```

